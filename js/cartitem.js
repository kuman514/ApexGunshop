class CartItem {
  constructor (_guntype, _name, _options, _price, _amount, _max) {
    this.type = _guntype
    this.name = _name
    this.options = _options
    this.price = _price
    this.amount = _amount
    this.maxAmount = _max
  }
  render () {
  }
}
